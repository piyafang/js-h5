canvas：
width、height属性

1.路径——范围

beginPath()	清除之前的路径
closePath()	闭合

线
moveTo/lineTo

圆弧
arc

矩形
rect

2.变换——越靠后越先出来
rotate
scale
translate

叠加
rotate(30);
rotate(30);

--60度

save()		保存状态(颜色、变换)，不保存图像
...
restore()

---------------------------------------------------------------------------------------

gd.save();
gd.beginPath();
画xxx
gd.restore();

---------------------------------------------------------------------------------------

像素操作——慢
1.获取
	getImageData(x, y, w, h)

2.放回去
	putImageData(imgData, x, y)

-------------

imgData.data数组

1个像素占4位——rgba	0-255

---------------------------------------------------------------------------------------

oC.toDataURL()	->	'base64字符串'

---------------------------------------------------------------------------------------

简易版
gd.drawImage(img, x, y)

完整版
gd.drawImage(img, sx, sy, sw, sh, dx, dy, dw, dh);

---------------------------------------------------------------------------------------

进去：
{
	xxx:	'地址'
}














