布局：
1.rem——相对于根(html)字体大小
	*只要调整html的字体大小，适应各种屏幕
2.盒模型
	box-sizing	往外、往里
	弹性布局、弹性盒模型
		父级：display:flex;
		子级：flex:数;

		父级加flex——1.让子级变成“浮动”状态；2.让子级变成box-sizing
		子级加flex——比例

		max/min

---------------------------------------------------------------------------------------

拖：
1.touchstart/move/end
2.绑定
3.坐标——
	ev.targetTouches[0].pageX

---------------------------------------------------------------------------------------

多点触摸：

方向：上下左右

手势：缩放、旋转

---------------------------------------------------------------------------------------

多点触摸：

identifier——ID
标识符

---------------------------------------------------------------------------------------

方向：
方向锁定——
1.方法——移动超过一段距离，固定在一个方向上
2.技巧——dir变量
	*作用1.确定方向
	*作用2.阻止程序再次判断——一旦在某一个方向上锁住了，不会再变

---------------------------------------------------------------------------------------

getComputedStyle——经过“计算”的样式
*处理后的结果

---------------------------------------------------------------------------------------

新的方法——彻底废了浏览器(自带的滚动完全没了)












