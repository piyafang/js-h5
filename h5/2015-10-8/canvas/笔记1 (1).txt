canvas——
1.像素操作——获取、放进去
2.游戏——性能高、捕鱼（难度）

drawImage(
	img,
	sx, sy, sw, sh,
	dx, dy, dw, dh
)

-------------

gd.strokeRect(100, 100, 300, 200);


gd.translate(250, 200);
gd.rotate(30);

gd.strokeRect(-150, -100, 300, 200);

---------------------------------------------------------------------------------------

canvas——JS

---------------------------------------------------------------------------------------

canvas：位图

矢量图
















