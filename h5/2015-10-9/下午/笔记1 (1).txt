VML——恶心
注意：
1.两个准备工作
	*加absolute
2.跟JS配合：
	事件——随便加
	属性——随便写
	样式——一样
	创建——一样
3.形状：
	line	from,to
	rect	style="width,height,left,top"
	oval	style="width,height,left,top"

	shape	path="M L X"
		*absolute
		**width,height=1000

---------------------------------------------------------------------------------------

兼容：
用JS添加VML的两个东西(xmlns、style)

---------------------------------------------------------------------------------------

oDiv.style.background='red';		√

---------------------------------------------------------------------------------------

<svg>
	line
	rect
</svg>

IE
<?>
	line
	rect
</?>

---------------------------------------------------------------------------------------

图形库——raphael

---------------------------------------------------------------------------------------

raphael——跟jQuery挺像

---------------------------------------------------------------------------------------

1.创建画布
Raphael(x,y,w,h)

2.形状
p.rect(x,y,w,h);
p.circle(cx, cy, r);
p.ellipse(cx, cy, rx, ry);
p.text(x, y, str);		位置：中心点
p.path
	A rx ry x旋转 是否大弧 顺时针 终点x 终点y

---------------------------------------------------------------------------------------

事件：
形状.click(fn);
形状.unclick(fn);

形状.unclick();		//全删、危险

---------------------------------------------------------------------------------------

click()、mouseover/out()、dblclick()、mousedown/move/up()

hover()

---------------------------------------------------------------------------------------

运动：

---------------------------------------------------------------------------------------

1.过程
var p=Raphael(x, y, w, h);

p.rect/circle/path

2.设置样式、属性
rect.attr('width', '400');
rect.attr('width')

3.事件
rect.click(fn);
rect.unclick(fn);

4.运动
rect.animate({}, 时间, 方式)
*方式：linear、ease、bounce、back、elastic

5.transform
rect.transform('t x,y s r');
*也能用于animate

---------------------------------------------------------------------------------------

属性、样式：
fill、stroke、stroke-width、path

---------------------------------------------------------------------------------------













