笔记
面向对象——————你会用，但是你不知道里面怎么回事。

****this是什么，不是定义决定的，而是调用决定的。

css中样式优先级
低 		     高
*<tab<class<id<行间

面向对象中的this
高 	new 		object
	定时器		window
	事件 		谁触发的事件，这个对象就是谁。
	方法		方法属于的那个对象
低	其他 		window/undefined



this的特点：
0.优先级
1.只看最后一步。
2.只管一层


------------------------------------------------
面向过程 	
面向函数	函数+函数+函数

面向对象 	对象+对象+对象

面向对象
--------------------------------------
1.onload				对象
2.所有的函数都提出来			方法
3.把所有其他方法中用到的变量变成	属性































































































