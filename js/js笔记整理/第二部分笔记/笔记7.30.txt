笔记
=======================================
预解析
js会把变量的声明（仅仅是声明），提到顶部。
不会突破作用域。

执行步骤:
读取一个script标签-》预解析-》执行里面的代码-》读取下一个script标签-》预解析-》执行里面的代码-》。。。。。

也不会突破script标签

函数也有预解析,但是要注意：
当以变量的形式声明的函数，预解析会按照变量的特性走。



为了性能。



======================================
引用: 	共同是一个东西：object 
只有在对象身上才会发生

数组的真正写法
new Array();


函数的真正写法
new Function();	//不推荐

外面用单引号里面就用双引号
外面用双引号里面就用单引号

计算机
每个字都有编码：
获取编码:
charCodeAt(下标)		获取某一位置字符的编码
获取字符串的编码
A	65
B	66
C	67

a	97
b 	98
c	99


1	49
2	50
3	51

通过编码获取字符
String.fromCharCode(编码)

1-250编码的字符是多少

中文
十进制
19968-40869
十六进制
4e00-9fa5
js
0x4e00
0x9fa5

有没有用？除了面试啥用都没有

加密：
获取到txt1的值
循环获取每个字符的编码
每个编码+3
变回字符串

str.charCodeAt()		获取某一位置的字符编码
String.fromCharCode()		通过编码获取字符串




报错的时候，后面的程序不执行。

bug异常	程序员不可控制的错误。
bug		臭虫

线上bug
try...catch

try	尝试着去执行一段代码。
如果没有做错。就正常执行。
如果有错，找catch

try{
	可能会出错的代码
}catch(e){
	补救代码
}
e	包含着错误信息

try....catch

应急用的。解决线上bug

关于声明变量。

当变量声明时，如果没加var，就变成全局的。

全局变量不好。
影响性能


逗号表达式,听后面的。
var  a = (12,5);
赋值的时候逗号表达式优先级很低。用括号提高优先级
alert((12,5));
alert是一个函数，我们相当于调用函数传参数。

**js中所有的全局的都是window的。

window的作用：在封闭空间中声明一个全局的变量

*this，谁触发的事件，这个this就是那个元素（对象）

*js语言本身存在的bug,js的bug跟山一样多。
	作者:布兰登·艾克（Brendan Eich）
		java
	为什么有这些bug？


	为什么不解决这个bug？

	微软：盖茨
	goole

	特别的聪明：
	严格模式
	'use strict'

	严格模式的好处:
	1.解决了局部函数中this是window的bug.
	2.解决了不加var声明变量的bug.
	3.避免在if，switch，for，while，forin里面定义函数
	4.去掉了with


*严格模式是好东西
每个js文件都加上严格模式	推荐



严格模式有作用域
1.函数
2.script


严格模式兼容?
高级浏览器兼容	ie9-不兼容

严格模式注意：
使用严格模式'use strict'前面不能有代码。

总结今天的东西。复习之前的东西。




